---
allowed-tools: Read, Glob, Grep, Edit
argument-hint: [focus]
description: 根据当前代码更新 CODEBUDDY.md 项目上下文
---

读取当前项目结构和关键配置，更新 `CODEBUDDY.md` 中过期的技术栈、目录边界、常用检查、跨层契约和安全规则。

要求：
1. 不记录真实密钥和本地 `.env` 内容。
2. 不把临时实现细节写成长期规则。
3. 只沉淀 AI 下次必须继承的项目规矩。
4. 保持内容短，优先保留会影响决策的信息。
