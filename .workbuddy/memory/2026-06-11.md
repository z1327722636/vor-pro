# 2026-06-11

- Implemented screenshot/image annotation for manual Lineup upload: users can draw arrows/rectangles, add notes over images, and the client exports annotated JPEGs before multipart upload.
- Added minimap coordinate fields to API responses and create/update/manual flows, plus frontend minimap visualization for standing point, landing point, and trajectory line.
- Improved mobile layouts for Lineup list/detail, upload workflow, frame picker, account/admin management pages, and added small-screen input font sizing to avoid iOS zoom.
- Validation run: frontend typecheck passed with non-incremental tsc; frontend lint passed with existing img/TypeScript-version warnings; backend app compiled successfully. Ruff unavailable in the managed Python env.
- Enhanced image annotations with per-shape style controls: selectable line/box thickness and arrow-head size, persisted on each annotation and respected by Canvas export.
- Replaced manual minimap coordinate inputs with an interactive map picker using official Valorant map icons for all 11 supported maps; upload, video draft, account edit, admin edit, and detail visualizer now use map-based point selection/display.
- Refined annotation style presets after visual review: reduced max stroke/arrow-head multipliers, lowered rectangle fill opacity, hid arrow-size controls when box tool is active, and aligned Canvas export with the subtler preview style.
